# ECA server binary — native GraalVM build from the official ECA tap.
# Install everything with:
#   brew bundle --file Brewfile
tap "editor-code-assistant/brew"
brew "editor-code-assistant/brew/eca"
